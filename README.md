# installation

```lua
return {
  "francivaldo4334/swagger.vim",
  dependencies = {
    "kkharji/sqlite.lua",
    "nvim-lua/plenary.nvim",
  },
  config = function() require("swagger").setup() end,
}
```
